<section class="events bs-docs-section">
  <div class="row">
    <div class="col-md-12">
      <div class="bs-component">
        <table class="table table-striped table-hover ">
          <thead>
            <tr>
              <th><%= Event.human_attribute_name(:date) %></th>
              <th><%= Event.human_attribute_name(:title) %></th>
              <th><%= Event.human_attribute_name(:location) %></th>
              <th><%= Event.human_attribute_name(:num_of_attendees) %></th>
              <th><%= Event.human_attribute_name(:total_cost) %></th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            <% @events.each do |event| %>
              <tr>
                <td><%= event.date %></td>
                <td><%= event.title %></td>
                <td><%= event.location %></td>
                <td>%</td>
                <td>%</td>
                <td>
                  <%= link_to material_icon.mode_edit.md_18, event_path(event) %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
        <% if admin_access? %>
          <nav>
            <%= link_to new_event_path, class: 'btn btn-primary btn-fab' do %>
              <%= material_icon.add %>
            <% end %>
          </nav>
        <% end %>
      </div>
    </div>
  </div>
</section>